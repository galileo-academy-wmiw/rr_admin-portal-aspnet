@page "/Orders-overview"
@model AdminPortal.Pages.Orders.IndexModel

@{
    ViewData["Title"] = "Orders";
}

<h1>Orders</h1>

@if (Model.Orders.Count == 0)
{
    <div class="alert alert-info">
        No Orders were found.
    </div>
}
else
{
<table class="table table-bordered">
    <thead>
        <tr>
            <th>Order ID</th>
            <th>Customer ID</th>
            <th>Customer First Name</th>
            <th>Customer Last Name</th>
            <th>Order Date</th>
            <th>Order Status</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody> @foreach (var order in Model.Orders)
        {
        <tr>
            <td>@order.OrderId</td>
            <td>@order.CustomerId</td>
            <td>@order.Customer.User.FirstName</td>
            <td>@order.Customer.User.LastName</td>
            <td>@order.OrderDate.ToString("dd-MM-yyyy")</td>
            <td>@order.OrderStatus</td>
            <td>
                <form method="post"
                      asp-page-handler="Complete"
                      asp-route-id="@order.OrderId">
                    <button type="submit" class="btn btn-primary">Complete</button>
                </form>

                <form method="post"
                      asp-page-handler="Reject"
                      asp-route-id="@order.OrderId">
                    <button type="submit" class="btn btn-danger">Reject</button>
                </form>
            </td>
        </tr>
        }
    </tbody>
</table>

}
